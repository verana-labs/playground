name: "Verandia 04 · Tax Buro (relying party)"

# Deploys the Republic's tax portal with a VERIFIER registration on the Verandia
# Citizen ID and an open VERIFIER permission on Legal Representative.
# Part of the Verandia cast CI/CD — see .github/workflows/verandia/README.md.

on:
  workflow_dispatch:
    inputs:
      step:
        description: "Which step to run"
        required: true
        type: choice
        options:
          - deploy
          - provision
          - all
        default: all
      force_refresh:
        description: "Re-issue credentials even when already linked"
        required: false
        type: boolean
        default: false

jobs:
  tax-buro:
    uses: ./.github/workflows/verandia-00_core.yml
    with:
      org: tax-buro
      step: ${{ inputs.step }}
      force_refresh: ${{ inputs.force_refresh }}
    secrets: inherit
