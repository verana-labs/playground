# =============================================================================
# Vesta Portal (demo) — verifiable login service (sub-service of Vesta)
# =============================================================================
# Delegated ECS-Service issued by the Vesta anchor; inherits Vesta's
# ECS-Organization. Holds a VERIFIER permission on ECS-Badge so partner
# technicians can log in by presenting their badge.

USER_ACC="vesta-playground-admin"
RELEASE_NAME="vesta-portal"
INGRESS_HOST="portal.vesta.playground.__NETWORK__.verana.network"
PROVISION_SCRIPT="provision-portal.sh"
EXPOSE_ADMIN_API="false"

# vs-agent prerelease under test (overrides the template's image tag)
VS_AGENT_IMAGE_TAG="v1.12.0-oidc4vc-uiprofile.30"

# Service credential claims (issued by the Vesta anchor)
SERVICE_NAME="Vesta Portal (demo)"
SERVICE_TYPE="VerifierService"
SERVICE_DESCRIPTION="Authorized Repairer login portal of Vesta Appliances (demo)"
SERVICE_LOGO_URL="https://raw.githubusercontent.com/verana-labs/playground/main/public/images/cast/vesta-portal.svg"
SERVICE_MIN_AGE="0"
SERVICE_TERMS="https://verana-labs.github.io/governance-docs/EGF/example.pdf"
SERVICE_PRIVACY="https://verana-labs.github.io/governance-docs/EGF/example.pdf"

# BADGE_SCHEMA_ID=""

# OpenID4VC rail (SD-JWT badge presentations): switches the deploy to the
# openid4vc image/chart and injects the verifier config. The badge issuers
# whose development-signing fingerprints get pinned at render time - all of
# them must already run the openid4vc image (deploy them before the portal).
# Umbra is pinned on purpose: its badge must reach the portal and be denied
# on the membership rule, not die on certificate pinning.
OID4VC_ROLE=verifier
OID4VC_ISSUER_RELEASES="vesta zenith umbra"
