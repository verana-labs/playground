{
  "$id": "https://northbank.bhi.playground.testnet.verana.network/schemas/employment.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EmploymentCredential",
  "description": "One employment relationship, established from HMRC payroll records by a certified DVS provider under the DUAA 2025 information gateway (demo). One credential per employment; the five-year history is the set of these in a wallet. For demonstration only.",
  "type": "object",
  "properties": {
    "credentialSubject": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "DID of the credential holder"
        },
        "employer": {
          "type": "string",
          "description": "Name of the employer"
        },
        "startDate": {
          "type": "string",
          "description": "Employment start date (ISO 8601)"
        },
        "endDate": {
          "type": "string",
          "description": "Employment end date (ISO 8601); absent for a current employment"
        }
      },
      "required": [
        "employer",
        "startDate"
      ]
    }
  }
}
